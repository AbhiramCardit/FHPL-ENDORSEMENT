export default function DashboardPage() {
  return (
    <div>
      <h1>Dashboard</h1>
      <p>Today's file counts, submission rates, pending reviews, and pipeline health.</p>
      {/* TODO: Implement dashboard widgets */}
    </div>
  );
}
